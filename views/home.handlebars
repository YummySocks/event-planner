<!-- Home Page -->
<div class="container-fluid homepage">
  <div class="row align-items-start">
    <nav class="col-2 bg-light homepage p-2">
      <h2> Navigation </h2>
          <button id="logout" type="button" class="btn btn-secondary"><a href="/login">Log Out</a></button> <script src='/js/logout.js'></script>
      <button type="button" class="btn btn-secondary"><a href="/new">Create New Event</a></button>
    </nav>
    <div class="col p-2">
      <h2>My Events</h2>
      {{#each listEvents}}
      <article>
        <div>
          <h2>
            <a href="/event/{{event_unique}}">{{title}}</a>
          </h2>
          <div>
          </div>
        </div>
        <div>
          {{description}}
        </div>
      </article>
      {{/each}}
    </div>
    <aside class="col-2 bg-light homepage p-2">
      <h2>My RSVPs</h2>
    </aside>
  </div>
  </div>
  <script src="/js/home.js"></script>
  <!-- end Home Page -->
